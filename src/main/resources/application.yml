spring:
  application:
    name: my
  main:
    allow-bean-definition-overriding: true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/c2c?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Hongkong
    username: root
    password: qwer53231323
  thymeleaf:
    cache: false
    suffix: .html
    mode: HTML5
    encoding: UTF-8


mybatis:
  mapper-locations: classpath:mapping/*.xml

logging:
  level:
    root: info
    com:
      wsk: debug
      netflix:
        discovery:
          shared:
            resolver:
              aws: error
server:
  port: 8888
